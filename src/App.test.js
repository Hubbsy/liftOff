// import React from 'react';
// import { render } from '@testing-library/react';
// import App from './App';

test('all content renders to the page', () => {
  // const { getByText } = render(<App />);
  // const linkElement = getByText(/map/i);
  // expect(linkElement).toBeInTheDocument();
});
